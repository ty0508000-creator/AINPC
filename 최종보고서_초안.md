# 2026-1학기 PD학기제 최종보고서 — 본문 초안

> **사용법**
> - 이 파일은 **HWP 본문에 붙여넣을 텍스트 초안**입니다. 공식 표지·헤더·지도교수 피드백 양식은 네 HWP 템플릿 그대로 두고, `Ⅰ. 서론`부터 본문만 채우면 됩니다.
> - `【채움: …】` = **너만 채울 수 있는 부분**(스크린샷·실측 수치·외부 출처). 지어내지 않고 비워둠.
> - `【확인: …】` = 사실관계 한 번 확인하고 지우면 되는 부분.
> - `[그림 N-N]`, `[표 N-N]` = 그림/표 들어갈 자리(양식의 그림목차·표목차와 번호 맞춤).
> - 서식: 큰제목 16pt·중간제목 14pt·본문 12pt·휴먼명조·줄간격 160%·양쪽정렬(양식 규정).

---

## ⚠️ 목차 조정 안내 (먼저 읽기)

중간보고서 §3에 적어둔 최종 목차 중 **실제 코드에 없는 항목 2개**가 있어요. 거짓으로 쓸 수 없으니 아래처럼 처리했습니다. 결정해서 알려주면 반영할게요.

| 원래 목차 항목 | 코드 현실 | 이 초안의 처리 |
|---|---|---|
| III-2-다 데이터 주도형 UI 및 **인벤토리** | UI·데이터주도(ScriptableObject)는 있음, **인벤토리 미구현** | "데이터 주도형 설계와 UI 시스템"으로 제목 수정, 인벤토리는 IV-3 향후계획으로 이동 |
| III-2-라 **오브젝트 풀링** 메모리/렌더링 최적화 | **미구현**(Instantiate/Destroy + 스폰포인트 재사용) | "몬스터 스폰·최적화"로 대체, 풀링은 IV-3 향후계획으로 이동 |

> 👉 만약 최종 제출 전에 **인벤토리/풀링을 실제로 구현**할 거면 말해줘. 그 경우 항목을 그대로 살려서 채울게.
>
> 📐 또한 지도교수 강조사항(게임 수학·물리)을 살리기 위해 **`Ⅲ-2-다. AI 전투·이동에 적용된 게임 수학과 물리`** 소절을 신설했습니다. 이에 따라 기존 데이터/UI·전투·레벨업 소절은 각각 **라·마·바**로 한 칸씩 밀렸습니다.

---

# Ⅰ. 서론

## 1. 프로젝트 개요

### 가. 연구 배경 및 목적

전통적인 게임의 NPC(Non-Player Character)는 미리 작성된 대사 트리(dialogue tree)와 결정론적 분기 구조에 의존한다. 이러한 방식은 제작 단계에서 모든 상호작용을 사전에 정의해야 하므로, 플레이어의 자유로운 입력에 유연하게 반응하지 못하고 상호작용의 결과가 항상 동일하다는 근본적 한계를 가진다. 최근 대규모 언어 모델(LLM, Large Language Model)의 발전은 이러한 한계를 넘어, 정해진 스크립트 없이도 맥락에 따라 대사를 생성하는 NPC를 가능하게 한다.

본 프로젝트는 **로컬 환경에서 구동되는 LLM을 Unity 게임 엔진과 연동하여, 사전 스크립트가 없는 생성형 AI NPC를 구현**하는 것을 목적으로 한다. 나아가 단순히 "대화하는 NPC"에 머무르지 않고, **AI의 판단이 게임의 물리적 수치와 캐릭터의 제어권에 직접 개입하는 비대칭적 게임 메커니즘**을 설계하여, AI가 게임 플레이 자체를 변화시키는 새로운 형태의 경험을 탐구한다.

구체적으로, 플레이어 캐릭터는 하나의 몸을 **플레이어와 또 다른 인격인 '어둠(Darkness)'이 공유**한다. 평소에는 플레이어가 캐릭터를 조작하지만, 특정 조건(전투 중 기분 게이지 저하)이 충족되면 AI 인격이 제어권을 강탈하여 스스로 행동한다. 플레이어는 '내면의 공간'에서 AI와 대화·협상하여 제어권을 되찾아야 한다. 이 과정에서 LLM의 대화 결과가 게임 상태(기분 게이지)에 반영되고, 그 상태가 다시 제어권 전환을 좌우하는 순환 구조를 통해, AI와 게임 시스템이 긴밀하게 결합된 메커니즘을 구현하는 것이 본 연구의 핵심 목표이다.

### 나. 연구 범위 및 방법

본 프로젝트의 범위와 수행 방법은 다음과 같다.

- **게임 클라이언트 개발**: Unity 엔진 기반 2D 탑뷰 RPG 환경 구축(타일맵 레벨, 플레이어 컨트롤러, 전투, 몬스터, UI).
- **로컬 LLM 연동**: 외부 상업 API(OpenAI·Gemini 등) 대신, 비용·보안·오프라인 구동을 고려하여 **로컬에서 구동 가능한 경량 LLM**을 Unity에 통합(LLMUnity 패키지 활용).
- **AI 인격 파인튜닝**: '어둠' 인격이 일관된 톤과 정해진 출력 형식(JSON)으로 응답하도록 소형 LLM을 **QLoRA 기법으로 파인튜닝**하고, GGUF 포맷으로 양자화하여 게임에 탑재.
- **제어권 전환 메커니즘**: LLM 대화 결과(기분 게이지)와 게임 상태(전투 여부)에 따라 플레이어↔AI 제어권이 전환되는 시스템을 책임 분리 원칙에 따라 설계·구현.
- **장기 기억(RAG)**: 과거 대화를 벡터 DB에 저장하고 의미 기반으로 회상하여, NPC가 플레이어와의 지난 상호작용을 기억하도록 구현.

방법론적으로는 MVP(Minimum Viable Product)를 우선 완성한 뒤 기능을 점진적으로 고도화하는 반복적 개발(iterative development) 방식을 채택하였으며, 공식 문서·커뮤니티 사례를 근거로 기술 선택을 검증하였다.

### 다. 프로젝트 진행 경과

전체 16주차 진행 경과를 요약하면 다음과 같다. 중간보고(8주차) 이전은 게임 클라이언트의 기반을 구축하는 단계였고, 이후는 **AI 통합·파인튜닝·제어권 메커니즘·완성도 향상**에 집중하였다.

[표 1-1] 주차별 진행 경과 요약

| 주차 | 핵심 활동 |
|---|---|
| 1 | 프로젝트 기획 구체화, 비대칭 게임 루프·관계형 시나리오 설계 |
| 2 | 게임 컨셉(황폐화된 마을) 확정, 2D 에셋 선정 및 AI 생성 |
| 3 | 타일셋 제작(흙 47·풀 9·애니메이션 물), 섬 형태 레벨 디자인 |
| 4 | 플레이어 컨트롤러 구현(Input System + Rigidbody2D 물리 이동) |
| 5 | NPC 배치 및 Animator 상태 머신(Idle/Walk) 연결 |
| 6 | 플레이어-NPC 근접 상호작용 및 대화창 자동 활성화 |
| 7 | 대화 UI 시스템 완성(입력창·출력창·스크롤·타이핑 효과), 1차 중간 점검 |
| 8 | 로컬 LLM 도입(Ollama 테스트, Llama-3-8B, 4bit 양자화) |
| 9 | LLMUnity 연동 및 모델 비교·선정 |
| 10 | NPC 페르소나 시스템 프롬프트 작성 및 튜닝 |
| 11 | **'어둠' 인격 LLM 파인튜닝(QLoRA)**, 몬스터·공격 구현 |
| 12 | 제어권 강탈 시스템 통합, 기능 통합 테스트 |
| 13 | 게임적 요소(배경음악·효과음) 추가 |
| 14 | 버그 수정 및 실행 파일(.exe) 빌드 테스트 |
| 15 | 최종 보고서·시연 영상 제작, 과제 제출 |
| 16 | 보고서 발표 |

【확인: 9~16주차 실제 진행과 위 요약이 맞는지 한 번 검토. 중간보고서의 "수정 계획"표를 실제 한 일로 바꿔 적은 것임.】

## 2. 게임 소개

### 가. 기본 정보

[표 1-2] 게임 기본 정보

| 항목 | 내용 |
|---|---|
| 제목 | 【채움: 게임 타이틀명】 |
| 장르 | 2D 탑뷰 액션 RPG |
| 플랫폼 | PC (Windows 스탠드얼론 .exe) |
| 엔진 | Unity 【채움: 버전, 예 2022.3 LTS】 |
| 조작 | 키보드(WASD 이동) + 마우스(좌클릭 공격·우클릭 대시) |
| 핵심 기술 | 로컬 LLM(LLMUnity), QLoRA 파인튜닝, RAG 장기 기억 |
| 개발 인원 | 2명 (이정택·김정현) |

### 나. 게임 세계관

게임의 배경은 **'대마왕의 침공으로 황폐화된 마을'**이다. 저채도와 어두운 톤으로 구성된 디스토피아적 세계 속에서, 플레이어 캐릭터는 친근하고 인간적인 외형을 지녔지만 내면에 **'어둠'이라는 또 다른 인격**을 품고 있다. 이 이중인격 설정은 캐릭터가 겪는 삶의 고난을 상징하며, 어둠이 표면화될 때 캐릭터의 눈동자가 붉게 변하고 화면에 글리치 효과가 나타나는 시각적 장치로 표현된다.

세계는 육지를 깊은 물로 둘러싼 **섬 형태**로 디자인되어, "물을 건널 수 없다"는 자연스러운 환경적 근거로 플레이 구역을 제한하면서도 몰입감을 해치지 않는다.

【채움: 게임 세계관 설정을 1~2문단 더 보강하면 좋음 — 어둠 인격의 기원, 마을 NPC들과의 관계 등.】

[그림 2-1] 게임 월드 전경(섬 형태 맵)
[그림 2-2] 플레이어 캐릭터 및 이중인격 시각 연출(눈 색 변화·글리치)

### 다. 게임 메커니즘 및 특징

본 게임의 차별점은 **AI가 관전자가 아니라 게임의 능동적 행위자**라는 점이다. 핵심 메커니즘은 다음 세 가지다.

1. **제어권 강탈(Takeover)**: 전투 중 기분 게이지가 임계치 이하로 떨어지면 AI 인격이 캐릭터의 제어권을 빼앗아 스스로 전투한다. AI는 강력하지만 몸을 사리지 않아 플레이어의 HP를 소모시키는 **위험-보상 트레이드오프**를 만든다.
2. **내면의 협상(Inner Dialogue)**: 제어권을 둘러싸고 플레이어는 '내면의 공간'에서 어둠과 LLM 기반 대화로 협상한다. 대화의 질이 기분 게이지를 좌우하고, 이것이 다시 제어권 전환을 결정한다.
3. **기억하는 NPC(RAG Memory)**: NPC는 과거 대화를 장기 기억하여, 플레이어의 이전 태도나 '어둠이 드러났던 사건'을 기억하고 이후 반응에 반영한다.

---

# Ⅱ. 게임 기획

## 1. 기존 게임 NPC 시스템의 한계 분석

### 가. 상호작용의 결정론적 구조와 한계

전통적 NPC는 사전 정의된 대사 트리를 따른다. 플레이어의 선택지는 제작자가 미리 마련한 분기 안에서만 가능하며, 같은 선택은 항상 같은 결과로 이어진다. 이는 제작 비용을 폭증시킬 뿐 아니라(모든 경우의 수를 수작업으로 작성), 플레이어가 트리의 경계를 체감하는 순간 몰입이 깨진다. 자유로운 자연어 입력에 대응하지 못한다는 점이 가장 큰 제약이다.

### 나. 서사적 연속성 및 상황 인지 능력 결여

기존 NPC는 대개 **상태를 기억하지 못한다**. 플레이어가 직전에 무슨 말을 했는지, 과거에 어떤 관계를 맺었는지를 누적하지 못하므로, 매 대화가 단절적이다. 또한 현재 게임 상황(전투 중인지, 체력이 위급한지)을 인지하지 못해, 맥락과 어긋나는 반응을 보인다.

### 다. 기술적·운영적 제약 요소

생성형 AI를 NPC에 적용할 때 상업용 API에 의존하면, **지속적 호출 비용**과 **네트워크 지연**, **오프라인 불가**, **데이터 프라이버시** 문제가 발생한다. 특히 학생 개발 환경에서 비용은 치명적 제약이며, 응답 지연은 게임의 몰입을 직접 저해한다.

## 2. 게임 시장 분석

【채움: 이 절은 외부 자료 조사가 필요합니다. 아래 골격에 출처와 함께 살을 붙여주세요.】

- **생성형 AI NPC 동향**: 최근 AI를 NPC 대화에 접목한 사례(예: Inworld AI, NVIDIA ACE, 일부 인디 타이틀 등)와 그 한계.
- **로컬 LLM 게임 적용 흐름**: 온디바이스·오프라인 추론에 대한 수요 증가, 양자화 모델 생태계.
- **인디게임 시장에서의 차별화 가능성**: 본 프로젝트가 노리는 틈새(AI가 게임 메커니즘에 직접 개입).

[표 2-1] 유사 사례 비교 분석 【채움: 사례별 방식·장단점 비교표】

## 3. 기존 기획의 문제점 분석

### 가. 초기 기획의 설계상 한계

초기에는 구현이 간편한 **외부 API 연동**을 전제로 기획하였다. 그러나 검토 과정에서 (1) 지속적 비용 부담, (2) 네트워크 지연에 따른 몰입 저하, (3) 외부 의존으로 인한 배포·보안 문제가 드러났다. 또한 AI를 단순 대화 상대로만 두는 초기 설계는, 프로젝트의 핵심 차별성(AI의 게임 개입)을 충분히 살리지 못한다는 점이 한계로 지적되었다.

### 나. 지도교수 조언

중간 면담에서 지도교수로부터 **"게임 수학 및 물리 원리의 명확한 이해와 적용"**을 강조하는 조언을 받았다. 이에 따라 부자연스러운 이동을 게임 수학 기반으로 재정비하는 것을 최우선 과제로 삼았으며(벡터 연산·가속도 적용), 향후 투사체 시스템에도 삼각함수 기반 궤적 계산과 물리 역학 공식을 엄격히 적용하는 방향으로 개발 공정을 조정하였다.

## 4. 문제 해결을 위한 심화 학습 및 연구

핵심 기술 선택을 임의로 하지 않고, 공식 문서와 비교 분석을 근거로 결정하였다.

**(1) 입력 시스템: Input Manager vs Input System**
2D 탑뷰 RPG는 이동·상호작용·스킬 등 다양한 입력과 상태 전환(필드/전투/UI)을 요구한다. Unity 공식 문서가 Input Manager를 레거시로 분류하고 신규 프로젝트에 Input System을 권장함을 확인하고, **Input System**을 채택하였다. Action Maps로 상태별 입력을 일괄 전환할 수 있고, 키보드·패드 입력을 동일 로직으로 처리하며, 이벤트 기반으로 폴링 대비 성능·가독성이 우수하기 때문이다.

> "Important: Input Manager is a legacy feature and not recommended for new projects. For new projects you should use the Input System Package." (Unity 공식 매뉴얼)

**(2) 이동 방식: Rigidbody2D vs Transform.position**
`transform.position` 직접 변경은 물리 연산을 무시해 벽 통과(phasing)가 잦다. 반면 **Rigidbody2D** 기반 이동은 물리 엔진이 충돌을 처리하여 벽 통과를 막고, 대각선 접촉 시 자연스러운 미끄러짐을 제공한다. Unity 공식 문서 역시 Rigidbody 2D 사용을 권장한다.

> "Moving the Rigidbody 2D provides the best performance and ensures correct collision detection." (Unity 공식 매뉴얼)

**(3) LLM 구동 방식: 외부 API vs 로컬 LLM**
비용·지연·오프라인·보안을 고려해 **로컬 LLM**으로 방향을 전환하였다. 방대한 양자화 모델·최적화 라이브러리 생태계를 활용하면 학생 환경에서도 안정적 구축이 가능함을 확인하였고, VRAM 절감을 위한 **4bit 양자화(Quantization)** 기법을 학습하여 적용하였다.

[표 2-2] 기술 선택 비교 요약 (Input System/Rigidbody2D/로컬 LLM의 대안 대비 장단점)

## 5. 개선안 도출 및 목표 재설정

위 분석을 종합하여 기획을 다음과 같이 재설정하였다.

- **외부 API → 로컬 LLM(LLMUnity)** 전환으로 비용 0·오프라인·보안 확보.
- **단순 대화 NPC → AI의 게임 개입** 강화: 제어권 강탈·기분 게이지 연동을 핵심 메커니즘으로 승격.
- **일반 페르소나 → 전용 파인튜닝 모델**: '어둠' 인격 전용 모델을 QLoRA로 학습하여 톤·출력 형식의 일관성 확보.
- **단발 대화 → 장기 기억(RAG)** 도입으로 서사적 연속성 보강.

---

# Ⅲ. 프로토타입 개발

## 1. 개발 환경

[표 3-1] 개발 환경

| 구분 | 내용 |
|---|---|
| 게임 엔진 | Unity 【채움: 버전】 |
| 언어 | C# |
| 입력 | Unity Input System (신규 패키지) |
| 物理 | Rigidbody2D 기반 2D 물리 |
| LLM 통합 | LLMUnity (임베디드 llama.cpp, GGUF 인프로세스 로드) |
| 파인튜닝 | Google Colab(T4 GPU), Unsloth + TRL(SFTTrainer) |
| 모델 | Meta-Llama-3.1-8B-Instruct 기반 QLoRA → GGUF(Q4_K_M) |
| 장기 기억 | RAG(벡터 DB, 임베딩 기반 검색) |
| 개발 PC | 【채움: CPU/GPU/RAM 사양】 |

## 2. 핵심 기능 상세

### 가. LLM 기반 동적 의도 파악 및 상황별 대화

본 프로젝트의 LLM 대화는 두 갈래로 구현되었다. **일반 NPC 대화(DialogueManager)**와 **내면 인격과의 협상(InnerVoiceManager)**이다. 두 경우 모두 다음 공통 설계를 따른다.

- **구조화된 출력(JSON)**: LLM이 항상 `{"dialogue":"대사","mood_delta":0}` 형식으로 응답하도록 시스템 프롬프트로 강제하여, 게임 로직이 대사와 감정 수치(mood_delta)를 한 번의 추론으로 동시에 파싱한다. 파싱은 JSON 우선, 실패 시 정규식 폴백의 2단계 방어로 안정성을 확보하였다.
- **스트리밍 + 타임아웃**: 응답을 토큰 단위로 받아 미완성 JSON에서 `dialogue` 값만 추출해 **타자기 효과**로 표시한다. 동시에 `Task.WhenAny`로 제한 시간(기본 20초)을 두어, 응답이 지연되면 대화를 "침묵 속에 종료"시켜 **소프트락(무한 대기)을 방지**한다.
- **상황 인지**: 현재 HP·기분 게이지·주변 적 수·제어 주체를 시스템 프롬프트에 주입하여, NPC가 게임 상황에 맞는 반응을 생성한다.

**① 내면의 공간(InnerVoiceManager)** — AI 인격과의 협상 무대다. 일정 주기(기본 30초)마다 일정 확률로, 또는 제어권 상황과 맞물려 발동하며, 발동 시 `Time.timeScale=0`으로 게임을 완전히 정지시키고 어두운 보라색 오버레이로 **현실에서 벗어난 내면 세계**를 연출(페이드·스케일 전환)한다. 플레이어가 입력한 말에 대해 어둠이 응답하고, 그 결과 `mood_delta`만큼 기분 게이지가 변동한다. 협상 내용은 RAG에 기억으로 저장된다.

**② 일반 NPC 대화(DialogueManager)** — 마을 NPC와의 대화다. 플레이어 근접 시 대화창이 자동 활성화되며, NPC별 페르소나를 시스템 프롬프트로 부여한다. 특기할 점은 **어둠이 몸을 제어 중일 때 NPC가 대화를 거부하고 공포에 질린 반응**을 보이며, 그 사건을 기억에 남겨 이후 정상 복귀 후에도 플레이어를 경계한다는 것이다. 이는 제어권 상실이 전투뿐 아니라 **사회적 관계에도 대가를 남기도록** 설계한 장치다.

**③ '어둠' 인격 파인튜닝** — 어둠이 일관된 톤과 출력 형식을 유지하도록 소형 LLM을 직접 파인튜닝하였다.
- *데이터 생성(gen_data.py)*: 상황(HP·기분·적 수·제어자)을 무작위 조합하는 규칙 기반 생성기로 약 1,000개의 멀티턴 샘플을 자동 생성. 플레이어 발화를 항복·반항·달램·거래·모욕·협력 등 **12개 카테고리**로 분류하고 (발화↔반응↔mood_delta 범위)를 짝지어 톤·감정의 방향성을 일관화. 시드 고정(seed=42)으로 재현성 확보. 학습 데이터의 시스템 프롬프트를 런타임과 동일 형식으로 맞춰 학습-추론 분포 불일치를 최소화.
- *학습(train.py)*: 무료 GPU(Colab T4)에서 8B 모델을 학습하기 위해 베이스(Meta-Llama-3.1-8B-Instruct)를 4bit로 로드하고 저랭크 어댑터만 학습하는 **QLoRA** 적용. 주요 설정 — LoRA rank/alpha 16, 4 epochs, learning rate 2e-4(cosine), 유효 배치 8, max sequence length 2048, Unsloth + TRL(SFTTrainer).
- *배포*: 학습 어댑터를 병합해 **GGUF(Q4_K_M)**로 양자화하고, 이를 LLMUnity의 LLM 컴포넌트에 직접 로드하여 **외부 API 없이 완전 오프라인 인프로세스 추론**을 수행. 추론 파라미터(temperature 등)를 고정해 캐릭터 톤의 일관성을 확보.

[그림 3-1] 내면의 공간 대화 화면(스트리밍 타자기 효과·기분 변화 표시)
[그림 3-2] 어둠 제어 중 일반 NPC의 공포 반응
[그림 3-3] Colab 파인튜닝 및 GGUF 변환 결과
【확인: 최종 인게임 추론이 (a) LLMUnity가 GGUF 직접 로드인지 (b) Ollama 서버 경유인지 확정해 한 줄로 명시. 코드 기준으론 (a) LLMUnity 직접 로드.】

### 나. AI 주도적 캐릭터 제어권 강탈 시스템

게임의 가장 독창적인 메커니즘으로, **하나의 몸을 플레이어와 AI가 공유**하고 조건에 따라 제어권이 전환된다. 책임 분리를 위해 세 컴포넌트로 구성하였다.

**① 조건 판정(MoodSystem.cs)** — 기분 게이지(0~100, 시작 50)를 관리한다. 강탈은 무조건 일어나지 않고 **"전투 중 && 기분 ≤ 20"**일 때만, 반환은 **"전투 종료 || 기분 ≥ 70"**일 때 발생한다. `CombatWatch` 코루틴이 0.5초 간격으로 일정 반경(12) 내 적 레이어를 검사해 전투 상태를 갱신하고, 전투 진입·이탈 순간마다 조건을 즉시 재평가한다. 따라서 전투가 끝나면 기분이 낮아도 즉시 제어권이 반환되어, 강탈을 **"위기의 순간에만 일어나는 긴장된 사건"**으로 만든다.

**② 상태 중계(ControlManager.cs)** — MoodSystem의 강탈/반환 이벤트를 구독해 제어 주체 플래그(`IsPlayerControlled`)를 갱신하고 동일 이벤트를 재발행한다. 이 **느슨한 결합(이벤트 중계)** 구조 덕분에 플레이어 입력·AI 행동·연출 등 여러 시스템이 서로를 직접 알 필요 없이 제어권 전환에 반응한다.

**③ AI 자동 행동(AIController.cs)** — 강탈 시 가장 가까운 적을 처치하거나 도망칠 때까지 연속 추격·공격한다. 거리에 따라 근거리는 일반 공격, 원거리(대시 사거리 내·쿨다운 완료)는 대시 공격, 그 외는 걸어서 접근하도록 분기한다. 중첩 코루틴을 `yield return`으로 호출해 제어권 회복 시 모든 하위 동작이 한 번에 중단되도록 하여 **코루틴 누수를 방지**한다. 한편 AI는 강하지만 몸을 사리지 않아, 적 접촉 범위(1.6) 안에서 1초마다 스스로 피해(5)를 입는다. 이로써 **"AI에게 몸을 맡기면 적은 잘 처치하지만 대신 몸이 상한다"**는 위험-보상 트레이드오프를 구현하였다.

**④ 연출(TakeoverEffect.cs)** — 제어권이 넘어가거나 회복되는 순간을 플레이어가 명확히 인지하도록 **적색 화면 플래시(잔광 유지)·카메라 흔들림·프리셋 대사·효과음**을 트리거한다. 단순한 내부 상태 변화가 아니라 **체감되는 사건**으로 전달하는 것이 목적이다.

[그림 3-4] 제어권 강탈 순간 연출(적색 플래시·카메라 흔들림·대사)
[표 3-2] 제어권 전환 조건 및 컴포넌트 역할 요약

### 다. AI 전투·이동에 적용된 게임 수학과 물리

지도교수의 조언("게임 수학 및 물리 원리의 명확한 이해와 적용")에 따라, 본 절에서는 앞서 구현한 플레이어·AI·몬스터 시스템 전반에 적용된 수학·물리 원리를 집약하여 기술한다. 본 게임의 조작감·전투 판정·성장 곡선은 모두 명시적 수식에 근거하여 설계되었다.

**① 벡터 정규화 — 대각선 속도 보정**
키보드 이동에서 상하·좌우를 동시에 입력하면 방향 벡터는 (1, 1)이 되고 그 크기는 √2 ≈ 1.414가 되어, 보정하지 않으면 대각선 이동이 직선보다 약 1.4배 빨라진다. 이를 해결하기 위해 입력 벡터를 단위 벡터로 정규화하고(v̂ = v / |v|) 여기에 이동 속도를 곱하여(속도 = v̂ · s), 모든 방향의 이동 속도를 일정하게 고정하였다. 동일한 정규화 원리를 AI의 적 추격 방향((적 − 나).normalized)과 몬스터의 플레이어 추격 방향 계산에도 일관되게 적용하였다.

**② 선형보간(Lerp)과 역보간(InverseLerp) — 대시 공격의 거리·피해 모델**
우클릭 대시 공격은 버튼을 누르는 시간을 비율 r = clamp(t / t_max), r ∈ [0, 1]로 환산한 뒤, 사거리와 피해를 이 비율에 선형 비례시킨다.
- 대시 사거리: d = lerp(d_min, d_max, r) = d_min + (d_max − d_min) · r  (1 ~ 7)
- 대시 피해: P = round(lerp(P_min, P_max, r))  (5 ~ 25)

따라서 짧게 누르면 가깝고 약하게, 오래 누르면 멀고 강하게 돌진하는 직관적 조작감을 수식으로 보장한다. 특히 제어권을 강탈한 AI가 대시할 때는, 적까지의 실제 거리 D로부터 역보간으로 충전 비율을 역산하여(r = InverseLerp(d_min, d_max, D) = (D − d_min) / (d_max − d_min)), 적과의 거리에 정확히 맞는 돌진량을 자동으로 계산하도록 하였다.

**③ 삼각함수(각도) — 공격 판정의 방향 회전**
근접 공격은 항상 플레이어가 바라보는 방향으로 판정되어야 한다. 이를 위해 마우스 위치로부터 방향 벡터 dir = normalize(마우스 − 플레이어)를 구하고, 기준 축(↑)과의 부호 있는 회전각 θ = SignedAngle(↑, dir)를 계산하여 공격 판정 박스를 해당 각도로 회전시켰다. 판정 박스의 중심은 중심 = 플레이어 + dir · range로 두어, 바라보는 방향 앞쪽에 정확히 타격 영역이 생성되도록 하였다.

**④ 기하 충돌 판정 — 원·사각형·스윕**
상황에 따라 서로 다른 기하 형상의 충돌 질의를 사용하였다. 전투 감지와 접촉 판정에는 원형 영역(OverlapCircle, 반지름 기반)을, 근접 공격 판정에는 회전된 사각형(OverlapBox)을, 대시 공격에는 경로 전체를 따라 박스를 쓸어 검사하는 스윕 캐스트(BoxCast)를 적용하여, 빠르게 지나가는 중에도 경로상의 모든 적을 빠짐없이 검출하였다. 사거리 분기는 두 점 사이 거리 |a − b|를 임계값과 비교하여 결정한다.

**⑤ 등비수열 — 레벨업 성장 곡선**
플레이어의 레벨업 난이도는 등비수열로 모델링하였다. 다음 레벨에 필요한 경험치를 Eₙ₊₁ = Eₙ × 1.5로 정의하면, 일반항은 Eₙ = E₁ · 1.5ⁿ⁻¹ = 100 · 1.5ⁿ⁻¹의 기하급수적 곡선을 이룬다. 반면 최대 HP·마나는 레벨당 +20, +10의 등차수열로 증가시켰다. 이로써 초반에는 빠르게 성장하지만 후반으로 갈수록 요구 경험치가 급격히 늘어나는, 점증적 난이도 곡선을 의도적으로 설계하였다.

**⑥ 운동학 적분과 이징 — 물리 이동과 연출**
모든 이동은 고정 시간 간격(Δt)으로 호출되는 FixedUpdate에서 위치를 적분(p ← p + v̂ · s · Δt)하여, 프레임률에 의존하지 않는 안정적 물리 거동을 보장하였다. 또한 Rigidbody2D의 보간(Interpolate) 옵션으로 시각적 떨림을 제거하였다. 내면세계 진입·이탈 등 화면 전환 연출에는 부드러운 가감속을 위해 SmoothStep 이징 함수 s(t) = 3t² − 2t³ (t ∈ [0, 1])을 적용하여, 선형 변화의 기계적 느낌 대신 자연스러운 전환을 구현하였다.

【그림 권장: 대각선 속도 보정 — 정규화 전(√2배)/후(1배) 벡터 비교 다이어그램】
【그림 권장: 대시 충전 비율 r에 따른 사거리·피해 선형 그래프】
【그림 권장: 레벨별 요구 경험치 등비수열 곡선 그래프】

### 라. 데이터 주도형 설계와 UI 시스템

> 【원래 "인벤토리" 항목이었으나 미구현. 데이터 주도형 설계 + UI로 대체. 인벤토리 구현 시 이 절에 추가.】

**① 데이터 주도형 몬스터 설계** — 몬스터의 능력치(최대 HP·이동 속도·공격력·공격 사거리·공격 쿨다운·어그로 범위·경험치 보상)를 코드가 아닌 **ScriptableObject(MonsterData)**로 분리하였다. 공통 행동(추격·공격·사망)은 추상 클래스 `MonsterBase`에 두고, 고블린·버섯·해골·박쥐 4종이 이를 상속한다. 덕분에 새 몬스터를 코드 수정 없이 데이터 에셋 교체만으로 추가·조정할 수 있다.

**② 계층적 UI 구조** — 대화 UI는 `DialogueCanvas` 하위에 `DialoguePanel`을 부모로 두어, 대화 시작/종료 시 패널의 활성화 상태(`SetActive`)만으로 전체 UI를 일괄 제어한다. 2D 픽셀 아트 분위기를 해치지 않도록 반투명 패널을 사용하고, 시선 이동을 고려해 좌측 대사창·우측 전송 버튼으로 배치하였다. UI는 런타임에 코드로 생성되어 씬 의존성을 줄였다.

**③ 사용자 중심 입력(UX)** — 플레이어 입력은 `TMP_InputField`로 즉시 표시되고, NPC 이름과 함께 답변이 출력되어 메신저 같은 연속적 대화 경험을 제공한다. AI가 답변을 생성하는 동안에는 전송 버튼을 비활성화하고 **"생각 중..."**을 노출하여 시스템 작동 상태를 명확히 인지시킨다.

[그림 3-5] 대화 UI 레이아웃(계층 구조)
[그림 3-6] 데이터 주도형 몬스터(MonsterData) 인스펙터

### 마. 전투 시스템과 몬스터

【원래 "오브젝트 풀링" 항목이었으나 미구현. 전투·몬스터로 대체. 풀링 구현 시 이 절에 최적화로 추가.】

**① 플레이어 전투(Player_Attack.cs)** — 두 가지 공격을 제공한다. 좌클릭 **일반 공격**은 마우스 방향으로 박스 범위(OverlapBox) 판정을 내려 데미지(10)를 주고 쿨다운(0.8초)을 둔다. 우클릭 **대시 공격**은 누르는 시간만큼 충전(최대 1.5초)되어 사거리(1~7)와 데미지(5~25)가 비례 증가하며, 대시 중에는 **무적(IsInvincible)**이 되고 BoxCast로 경로 상의 적을 타격한다(쿨다운 5초). 모든 피해 판정은 `IDamageable` 인터페이스로 추상화되어 플레이어·몬스터에 공통 적용된다. 공격은 `ForceAttack`/`ForceDash` 인터페이스로 노출되어 **제어권 강탈 시 AI도 동일한 전투 로직을 재사용**한다.

**② 몬스터 AI 및 스폰** — `MonsterBase`는 플레이어를 추격하다가 공격 사거리에 들면 쿨다운에 맞춰 공격하고, 사망 시 플레이어에게 경험치를 지급한다. 원거리 몬스터는 `MonsterProjectile`로 투사체를 발사한다. `MonsterSpawnArea`가 일정 간격(10초)으로 비어 있는 스폰 포인트를 검사해 몬스터를 재생성하여, 필드에 지속적으로 적이 유지되도록 하였다.

【확인: 게임 수학/물리 적용 사례(벡터 정규화로 대각선 속도 보정, 가속도, 삼각함수 궤적 등)를 이 절 또는 II-4에 한 단락 보강하면 지도교수 조언 반영이 잘 드러남.】

[그림 3-7] 전투 장면(일반 공격·대시 공격 판정 시각화)
[그림 3-8] 몬스터 4종 및 스폰 영역

### 바. 경험치 곡선 기반 레벨업 시스템

플레이어 성장(PlayerStats.cs)은 레벨·HP·마나·경험치로 구성된다. 몬스터를 처치하면 경험치를 획득하고, 누적 경험치가 다음 레벨 요구치에 도달하면 레벨이 오른다. 레벨업 시 **최대 HP(+20)·최대 마나(+10)**가 증가하고 HP·마나가 가득 찬다. 핵심은 다음 레벨 요구 경험치가 **이전 요구치의 1.5배로 증가하는 기하급수적 곡선(MaxEXP ← MaxEXP × 1.5)**이라는 점으로, 레벨이 오를수록 성장 난이도가 점증하도록 설계하였다. 모든 성장 수치는 세이브 시스템을 통해 저장되어 재실행 시 복원된다.

[표 3-3] 레벨별 요구 경험치·스탯 변화(곡선 예시)
[그림 3-9] 플레이어 HUD(레벨·HP·마나·경험치 바)

## 3. 개발 결과물 검증 및 테스트 결과

본 절은 실제 플레이 검증 결과를 담는다. 아래 항목을 실측·캡처하여 채워주세요.

- **기능 통합 테스트**: 대화→기분 변화→전투 중 강탈→내면 협상→제어권 반환의 전체 루프가 정상 동작하는지 시나리오 단위로 검증. 【채움: 테스트 항목별 통과/실패 표】
- **LLM 응답 성능**: 로컬 추론 1회 평균 응답 시간, 타임아웃 발생 빈도. 【채움: 실측 수치(초)】
- **프레임 성능**: 평상시·전투 시 평균 FPS. 【채움: 실측 수치】
- **트러블슈팅 기록**: (중간보고서 표 재활용)

[표 3-4] 주요 트러블슈팅

| 항목 | 발생 현상 | 해결 방안 |
|---|---|---|
| 대각선 속도 보정 | 상하좌우 동시 입력 시 이동 속도가 직선 대비 약 1.4배 빨라짐 | 입력 벡터에 `normalized` 적용하여 모든 방향 속도를 1로 고정 |
| 충돌 시 회전 | 모서리·장애물에 비스듬히 충돌 시 캐릭터가 회전 | Rigidbody2D의 Freeze Rotation Z 활성화 |
| 화면 떨림(Jittering) | 이동 시 캐릭터·카메라가 미세 진동 | Rigidbody2D Interpolate 적용 + 이동 로직을 FixedUpdate 주기에 정렬 |
| LLM 무한 대기 | 로컬 추론 지연 시 대화창이 멈춤 | `Task.WhenAny` 타임아웃(20초) 후 "침묵 종료"로 소프트락 방지 |

[표 3-5] 기능 통합 테스트 결과 【채움】

---

# Ⅳ. 결론

## 1. 프로젝트 성과 및 의의

- **명확한 비전과 일관된 개발 방향**: 초기에 세계관·기획을 확정하여 시행착오를 줄이고 개발 효율을 높였다.
- **독창적 AI 게임 메커니즘 구현**: AI NPC를 단순 챗봇이 아니라 캐릭터의 제어권과 물리 수치에 직접 개입하는 행위자로 설계하여, 기존 NPC의 결정론적 한계를 넘어서는 차별적 경험을 완성했다.
- **로컬 LLM 풀스택 통합**: 데이터 생성→QLoRA 파인튜닝→GGUF 양자화→Unity 인게임 추론까지 **비용 0·오프라인** AI 파이프라인을 학생 환경에서 端到端으로 구축했다.
- **서사적 연속성 확보**: RAG 장기 기억으로 NPC가 과거 상호작용을 기억하게 하여 몰입을 강화했다.

## 2. 개발 한계점 및 시사점

- **로컬 LLM 추론 속도**: 8B 모델의 로컬 추론은 응답 지연이 존재한다. 타임아웃으로 소프트락은 막았으나, 근본적 속도는 모델 경량화·하드웨어에 의존한다.
- **미구현 기능**: 인벤토리 시스템과 오브젝트 풀링 최적화는 기간 내 구현하지 못했다(아래 향후 계획 참조).
- **시사점**: AI를 게임의 "대화 장식"이 아니라 "시스템 행위자"로 통합할 때 비로소 차별적 재미가 발생함을 확인했다. 동시에 로컬 AI의 성능 제약을 게임 디자인(턴 정지·타임아웃 연출)으로 흡수하는 설계가 중요함을 배웠다.

## 3. 향후 발전 계획

### 가. AI 페르소나의 다변화 및 학습 데이터 고도화

현재 '어둠' 단일 인격에 더해 다양한 NPC 전용 페르소나 모델로 확장하고, 자동 생성 데이터의 카테고리·상황 다양성을 늘려 대화 품질을 고도화한다. Llama 외 최신 경량 모델(예: Gemma 계열) 및 튜닝 모델을 비교 실험하여 응답 속도·품질의 균형점을 찾는다.

### 나. 게임플레이 메커니즘의 차별화

미구현 시스템을 완성하여 깊이를 더한다. **(1) 인벤토리 시스템** — 데이터 주도형(ScriptableObject) 아이템 설계로 확장. **(2) 오브젝트 풀링** — 몬스터·투사체의 Instantiate/Destroy를 풀링으로 전환하여 메모리·렌더링 부하를 줄이고 GC 스파이크를 완화. **(3) 투사체 궤적** — 삼각함수 기반 궤적 계산 적용.

### 다. 상업적 확장 및 서비스 플랫폼화

스탠드얼론 빌드를 넘어 Itch.io 등 인디 플랫폼 배포를 검토하고, 사용자 피드백 기반으로 콘텐츠를 확장한다. 【채움: 구체적 배포·수익화 구상이 있으면 보강.】

---

# 참고문헌

【채움: 아래는 초안. 실제 인용 형식(예: 학술 인용)에 맞춰 정리하고 접속일자 기입.】

1. Unity Technologies, *Input System Package Documentation*. https://docs.unity3d.com/Packages/com.unity.inputsystem@latest
2. Unity Technologies, *Rigidbody 2D / 2D Physics Manual*. https://docs.unity3d.com/Manual/class-Rigidbody2D.html
3. Meta AI, *Llama 3 Model Card*. https://github.com/meta-llama/llama3
4. Dettmers, T. et al., *QLoRA: Efficient Finetuning of Quantized LLMs*, 2023. https://arxiv.org/abs/2305.14314
5. Unsloth, *Unsloth Documentation*. https://github.com/unslothai/unsloth
6. Hugging Face, *TRL: Transformer Reinforcement Learning (SFTTrainer)*. https://github.com/huggingface/trl
7. Ollama, *Ollama Documentation*. https://ollama.com
8. undreamai, *LLMUnity*. https://github.com/undreamai/LLMUnity
9. 【채움: 사용한 Itch.io 에셋 출처(타일셋·캐릭터·집 등)】
10. 【채움: 게임 시장 분석에서 인용한 자료】
